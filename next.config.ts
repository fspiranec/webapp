import type { NextConfig } from "next";

// Central Next.js runtime/build configuration.
// Keep this typed object explicit so future options get editor autocomplete + type checking.

const nextConfig: NextConfig = {
  // Intentionally minimal for now; add framework-level behavior here (images, headers, rewrites, etc.).
  /* config options here */
};

export default nextConfig;
